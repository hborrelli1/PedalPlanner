import React from 'react'
import PropTypes from 'prop-types'

const Charts = ({ userInfo }) => {
  // have access to props: userInfo => pastRides => trailId
  return (
    <div className='charts-wrapper'>
      Charts here.
    </div>
  )
}

export default Charts;
