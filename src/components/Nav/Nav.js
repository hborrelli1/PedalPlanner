import React from 'react'
import PropTypes from 'prop-types'

const Nav = (props) => {
  return (
    <header>
      <div>
        Logo Here.
      </div>
      <button>Login</button>
    </header>
  )
}

export default Nav
